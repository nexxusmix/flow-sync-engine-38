/**
 * Default workspace ID used as fallback across the application.
 * Single source of truth — import this instead of hardcoding UUIDs.
 */
export const DEFAULT_WORKSPACE_ID = '00000000-0000-0000-0000-000000000001';
