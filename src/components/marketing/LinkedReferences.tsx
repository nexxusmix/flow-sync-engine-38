// Re-export the new component for backwards compatibility
// The LinkedReferences component now uses the reference_links table via ReferenceLinksSection
export { ReferenceLinksSection as LinkedReferences } from "./ReferenceLinksSection";
